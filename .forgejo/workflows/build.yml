name: build

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build and release
    runs-on: docker
    container:
      image: cimg/android:2026.03-node
    env:
      ENGINE_VERSION: v0.3.0
    steps:
      - name: Install packaging tools
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends fakeroot dpkg dpkg-dev rpm

      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Install the Android platform the app compiles against
        run: |
          sdkmanager --install "cmdline-tools;latest"
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-37.0"

      - name: Set up Gradle
        uses: https://github.com/gradle/actions/setup-gradle@v5

      - name: Download the Reseam CLI
        uses: https://code.forgejo.org/actions/forgejo-release@v2.11.1
        with:
          direction: download
          url: https://git.reseam.app
          repo: reseam/reseam
          tag: ${{ env.ENGINE_VERSION }}
          token: ${{ secrets.RELEASE_TOKEN }}
          release-dir: engine

      - name: Install the signing keystore
        run: echo "${{ secrets.ANDROID_KEYSTORE_B64 }}" | base64 -d > keystore.jks

      - name: Build and stage the release
        env:
          RESEAM_BIN: engine/reseam-linux-x64
          RESEAM_KEYSTORE: ${{ github.workspace }}/keystore.jks
          RESEAM_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          RESEAM_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          RESEAM_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          chmod +x engine/reseam-linux-x64
          ./gradlew :composeApp:publishManagerIndex -PmanagerVersion=${{ github.ref_name }} --no-daemon

      - name: Upload to the Forgejo release
        uses: https://code.forgejo.org/actions/forgejo-release@v2.11.1
        with:
          direction: upload
          url: https://git.reseam.app
          repo: reseam/manager
          tag: ${{ github.ref_name }}
          sha: ${{ github.sha }}
          token: ${{ secrets.RELEASE_TOKEN }}
          release-dir: composeApp/build/release
          override: true
